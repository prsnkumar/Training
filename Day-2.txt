Change HEADER Line updated
=======
Login details:
# Better remove this credentials
=======
Login details:
https://github.com
prsnkumar
$prasanna123


…or create a new repository on the command line
echo "# poc" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:prsnkumar/poc.git
git push -u origin master


Commands
	- Push
	- Pull - first time we clone, subsequently we pull.
	- Fetch
	- Rebase

Git integration with JIRA



git config --local user.name "prsnkumar"
git config --local user.email "prsnkumar@gmail.com"

git config user.name
git config user.email




git clone git@github.com:prsnkumar/Training.git

git push origin
git push --set-upstream origin develop

=========
git pull --rebase origin master
git status
git mergetool
-Change the data
git rebase --continue
git push origin master

It is same as pull and merge, but rebase is to maintain clean history.
It maintains the linear path, instead of divergent paths.

